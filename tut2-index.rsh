'reach 0.1';
export const main = 
Reach.App(
{},
//The set of particiapants
[Participant('Alice', {}), Participant('Bob', {})], 
(A,B) => {
 	exit(); }); 
 	//Make Alice and Bob strings because Alice and Bob are actual parameters within in the function
 	// The user interacts with the main function and the chosen smart contracts to provide a Middleware that manages to interact with their computer.